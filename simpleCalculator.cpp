#include <iostream>
using namespace std;

int main(){
    int x, y;
    cout << "Type a number: ";
    cin >> x;
    cout << "Type in another number: ";
    cin >> y;

    cout << "The sum of " << x << " and " << y << " is " << x + y;

    return 0;
}